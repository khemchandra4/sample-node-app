# Sample Node App

This app is intended for testing GitHub repository URL deployment and build settings.

## Build

```bash
npm run build
```

## Start

```bash
npm start
```

The server listens on `PORT` if provided, otherwise `3000`.
